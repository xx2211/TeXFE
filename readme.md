主要功能：
1. 截取屏幕区域识别公式
2. 手机扫码上传图片,电脑端编辑选取区域识别公式
3. 编辑、预览识别结果

使用：

python解释器： 3.10  64位, 可以使用conda创建python环境

1. 执行 `pip install -r requirements.txt` 安装依赖
2. 执行 `python main.py` 启动程序
3. 启动后可以通过 1)托盘图标 或 2)快捷键alt+q、alt+m识别公式